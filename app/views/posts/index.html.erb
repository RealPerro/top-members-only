<h1>posts</h1>

<ul>
  <% @posts.each do |post| %>
    <li>
      <h3><%= link_to post.title, post_path(post) %></h3>
      <p><%= post.body %></p>
      <%if user_signed_in? %>
        <p>Author is: <%= post.user_id %>
      <% end %>
    </li>
  <% end %>
</ul>

